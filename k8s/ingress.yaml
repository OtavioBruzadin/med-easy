apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: med-easy-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: med-easy-service
                port:
                  number: 80